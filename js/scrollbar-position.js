/*eslint-env es6*/

/* scrollbar-position.js for Zach Feldman */

$(document).ready(function() {







    // const sidebar = $('.sidebar-container');

    // // Event handler for scrolling within the sidebar
    // sidebar.on('scroll', function() {
    //     const scrollPosition = sidebar.scrollTop();
    //     sessionStorage.setItem('sidebarScrollPos', scrollPosition);
    //     console.log('Sidebar scroll position saved:', scrollPosition); // Debugging message
    // });

    // // Function to restore the scroll position
    // function restoreScrollPosition() {
    //     // Retrieve the saved scroll position from sessionStorage
    //     const savedScrollPos = sessionStorage.getItem('sidebarScrollPos');
    //     if (savedScrollPos !== null) {
    //         // Convert the saved position back to a number and set the sidebar's scroll position
    //         sidebar.scrollTop(parseInt(savedScrollPos, 10));
    //         console.log('Sidebar scroll position restored to:', savedScrollPos); // Debugging message
    //     }
    // }

    // // Restore the scroll position when the page loads
    // restoreScrollPosition();


});